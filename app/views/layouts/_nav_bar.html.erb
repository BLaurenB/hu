<nav>
  <div class="nav">
    <div><site-title>Hū</site-title>

        <% if current_user %>
          Signed in as <%= current_user.authentication.name %>
          <%= link_to "Sign out", signout_path, id: "sign_out" %>
        <% else %>
          <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %>
        <% end %>

      </div>
    </div>
</nav>

<div class="center"><% flash.each do |key, value| %>
   <div class="alert alert-<%= key %>"><%= value %>
 <% end %>
</div>
