<nav>
  <div class="header box">
    <div class="nav-col-1 box "><site-title>Hū</site-title>
    </div>
      <div class="nav-col-4 box">
          <% if current_user %>
          <h3>Signed in as <%= current_user.authentication.name %></h3>
      </div>
      <div class="nav-col-6 box">
          <h3><%= link_to "Sign out", signout_path, id: "sign_out" %> </h3>
        <% else %>
          <h3><%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %></h3>
        <% end %>
      </div>

    </div>

</nav>

<div class="nav-col-3 nav-col-4 box"><% flash.each do |key, value| %>
   <div class="alert alert-<%= key %>"><%= value %>
 <% end %>
  </div>
</div>
